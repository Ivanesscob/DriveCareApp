<Window x:Class="DriveCareCore.Dialogs.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title=""
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Topmost="False"
        MinWidth="360"
        MaxWidth="480">

    <Window.Resources>
        <SolidColorBrush x:Key="Brush.Surface" Color="#252538"/>
        <SolidColorBrush x:Key="Brush.Border" Color="#3D3D55"/>
        <SolidColorBrush x:Key="Brush.Accent" Color="#7C6CF5"/>
        <SolidColorBrush x:Key="Brush.AccentHover" Color="#9488FF"/>
        <SolidColorBrush x:Key="Brush.Muted" Color="#9AA0B0"/>

        <Style x:Key="BtnPrimary" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="MinWidth" Value="88"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" CornerRadius="10" Background="{StaticResource Brush.Accent}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{StaticResource Brush.AccentHover}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnGhost" TargetType="Button" BasedOn="{StaticResource BtnPrimary}">
            <Setter Property="Foreground" Value="{StaticResource Brush.Muted}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" CornerRadius="10" Background="Transparent" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#332E55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Padding="28" Background="Transparent">
        <Border CornerRadius="18" Background="{StaticResource Brush.Surface}" BorderBrush="#5548C8" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="40" ShadowDepth="0" Opacity="0.55" Color="#000000"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Заголовок + закрыть -->
                <Grid Grid.Row="0" Margin="20,16,12,0" MouseDown="TitleBar_OnMouseDown">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="TitleText" Grid.Column="0"
                               Text="DriveCare" FontSize="16" FontWeight="SemiBold" Foreground="White"
                               TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                    <Button x:Name="BtnClose" Grid.Column="1" Width="32" Height="32" Padding="0"
                            Background="Transparent" BorderThickness="0" Foreground="{StaticResource Brush.Muted}"
                            FontSize="18" Cursor="Hand" Click="BtnClose_OnClick" ToolTip="Закрыть">
                        <TextBlock Text="×" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-4,0,0"/>
                    </Button>
                </Grid>

                <!-- Иконка + текст -->
                <Grid x:Name="MessageAreaGrid" Grid.Row="1" Margin="20,12,20,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition x:Name="IconColumn" Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Иконки (показывается одна) -->
                    <StackPanel x:Name="IconInfo" Grid.Column="0" Width="48" Height="48" Margin="0,2,16,0" Visibility="Collapsed">
                        <Border Width="48" Height="48" CornerRadius="24" Background="#2563EB">
                            <TextBlock Text="i" FontSize="22" FontWeight="Bold" Foreground="White"
                                       HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="Segoe UI"/>
                        </Border>
                    </StackPanel>
                    <StackPanel x:Name="IconWarning" Grid.Column="0" Width="48" Height="48" Margin="0,2,16,0" Visibility="Collapsed">
                        <Border Width="48" Height="48" CornerRadius="24" Background="#D97706">
                            <TextBlock Text="!" FontSize="24" FontWeight="Bold" Foreground="White"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </StackPanel>
                    <StackPanel x:Name="IconError" Grid.Column="0" Width="48" Height="48" Margin="0,2,16,0" Visibility="Collapsed">
                        <Border Width="48" Height="48" CornerRadius="24" Background="#DC2626">
                            <TextBlock Text="✕" FontSize="18" FontWeight="Bold" Foreground="White"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </StackPanel>
                    <StackPanel x:Name="IconQuestion" Grid.Column="0" Width="48" Height="48" Margin="0,2,16,0" Visibility="Collapsed">
                        <Border Width="48" Height="48" CornerRadius="24" Background="{StaticResource Brush.Accent}">
                            <TextBlock Text="?" FontSize="22" FontWeight="Bold" Foreground="White"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </StackPanel>

                    <ScrollViewer x:Name="MessageScrollViewer" Grid.Column="1" VerticalScrollBarVisibility="Auto" MaxHeight="220">
                        <TextBlock x:Name="MessageText"
                                   TextWrapping="Wrap"
                                   Foreground="#E8E8EF"
                                   FontSize="14"
                                   LineHeight="20"/>
                    </ScrollViewer>
                </Grid>

                <!-- Кнопки -->
                <StackPanel x:Name="ButtonPanel" Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right"
                            Margin="20,8,20,20">
                    <Button x:Name="BtnYes" Content="Да" Style="{StaticResource BtnPrimary}" Margin="0,0,8,0" Click="BtnYes_OnClick" Visibility="Collapsed"/>
                    <Button x:Name="BtnNo" Content="Нет" Style="{StaticResource BtnGhost}" Margin="0,0,8,0" Click="BtnNo_OnClick" Visibility="Collapsed"/>
                    <Button x:Name="BtnCancel" Content="Отмена" Style="{StaticResource BtnGhost}" Margin="0,0,8,0" Click="BtnCancel_OnClick" Visibility="Collapsed"/>
                    <Button x:Name="BtnOk" Content="ОК" Style="{StaticResource BtnPrimary}" Click="BtnOk_OnClick" Visibility="Collapsed"/>
                </StackPanel>
            </Grid>
        </Border>
    </Border>
</Window>
